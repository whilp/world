import imp
import nose
import sys

fname = sys.argv[1]
sys.argv = sys.argv[:1]
mod = imp.load_source("mod", fname)

loader = nose.loader.TestLoader()
suite = loader.loadTestsFromModule(mod)
nose.main(suite=suite)
