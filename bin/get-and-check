#!/bin/sh

src="$1"
dst="$2"
sum="$3"
curl -sLo "$dst" "$src"
got=$(shazam "$dst")
got="${got%% *}"
if [ "$got" != "$sum" ]; then
   echo "${dst}: expected SHA256 checksum '$sum', got '$got'" 
   exit 1
fi
