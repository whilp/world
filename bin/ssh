#!/bin/sh

case ${SITE} in
    simple)    args="-oUser=will";;
    *)      args="-oUser=will";;
esac
case "$@" in *@*) args=""; esac

for arg in $*; do host=$arg; done
#printf '\033k'$host'\033\\'
exec /usr/bin/${0##*/} $args $*
