#!/bin/sh

cd ~/
source ~/.profile

export PLAN9=~/plan9port
export PATH=$PATH:$PLAN9/bin
export PAGER=nobs
export SHELL=bash
export TERM=dumb

pgrep -q fontsrv || fontsrv &
pgrep -q plumber || plumber

exec acme -f /mnt/font/Monaco/14a/font -a
