set -s      escape-time         0
set -g      base-index          1
set -g      bell-action         any
set -g      default-command     "exec ${SHELL} -l"
set -g      history-limit       10000
set -g      prefix              C-t
set -g      pane-active-border-fg black
set -g      pane-active-border-bg black
set -g      message-bg          white
set -g      message-fg          black
set -g      status-bg           black
set -g      status-fg           white
set -g      status-right        " "
set -g      update-environment  "DISPLAY WINDOWID SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_CONNECTION TMUX_SESSION"
set -wg     automatic-rename    off
set -wg     mode-keys           vi

# Bindings reset; see below for actual keys.
unbind-key C-b
unbind-key C-o
unbind-key C-z
unbind-key Space
unbind-key !
unbind-key '"'
unbind-key "#"
unbind-key %
unbind-key &
unbind-key "'"
unbind-key ,
unbind-key -
unbind-key .
unbind-key 0
unbind-key 1
unbind-key 2
unbind-key 3
unbind-key 4
unbind-key 5
unbind-key 6
unbind-key 7
unbind-key 8
unbind-key 9
unbind-key :
unbind-key ?
unbind-key D
unbind-key [
unbind-key ]
unbind-key c
unbind-key d
unbind-key f
unbind-key i
unbind-key l
unbind-key n
unbind-key o
unbind-key p
unbind-key q
unbind-key r
unbind-key s
unbind-key t
unbind-key w
unbind-key x
unbind-key {
unbind-key }
unbind-key "~"
unbind-key PPage
unbind-key Up
unbind-key Down
unbind-key Left
unbind-key Right
unbind-key M-1
unbind-key M-2
unbind-key M-3
unbind-key M-4
unbind-key M-5
unbind-key M-n
unbind-key M-o
unbind-key M-p
unbind-key M-Up
unbind-key M-Down
unbind-key M-Left
unbind-key M-Right
unbind-key C-Up
unbind-key C-Down
unbind-key C-Left
unbind-key C-Right

bind -n M-Q select-window -t1
bind -n M-W select-window -t2
bind -n M-E select-window -t3
bind -n M-R select-window -t4
bind -n M-T select-window -t5
bind -n M-Y select-window -t6
bind -n M-U select-window -t7
bind -n M-I select-window -t8
bind -n M-O select-window -t9
bind -n M-P select-window -t10
bind Q select-window -t1
bind W select-window -t2
bind E select-window -t3
bind R select-window -t4
bind T select-window -t5
bind Y select-window -t6
bind U select-window -t7
bind I select-window -t8
bind O select-window -t9
bind P select-window -t10
bind 1 select-window -t1
bind 2 select-window -t2
bind 3 select-window -t3
bind 4 select-window -t4
bind 5 select-window -t5
bind 6 select-window -t6
bind 7 select-window -t7
bind 8 select-window -t8
bind 9 select-window -t9
bind 0 select-window -t10

bind -n M-q	lock-client
bind q lock-client

bind -n M-`	last-window
bind ` last-window
bind C-t last-window

bind -n M-h select-pane -L
bind h select-pane -L

bind -n M-j select-pane -D
bind j select-pane -D

bind -n M-k select-pane -U
bind k select-pane -U

bind -n M-l select-pane -R
bind l select-pane -R

bind -n M-H previous-window
bind H previous-window

bind -n M-J rotate-window -D
bind J rotate-window -D

bind -n M-K rotate-window -U
bind K rotate-window -U

bind -n M-L next-window
bind L next-window

bind -n M-N new-window
bind N new-window

bind -n M-. command-prompt "rename-window %%"
bind . command-prompt "rename-window %%"

bind -n M-m set-window-option monitor-activity
bind m set-window-option monitor-activity

bind -n M-s set-option status
bind s set-option status

bind i send-prefix
bind -n M-: command-prompt
bind : command-prompt
