#!/bin/sh

cmd="$1"
ssh_env="/ssh/env"
gpg_env="/gpg/env"
[ -d "${ssh_env}" ] && set -- chpst -e "${ssh_env}" "$@"
[ -d "${gpg_env}" ] && set -- chpst -e "${gpg_env}" "$@"

case "${cmd}" in
    *) exec "$@";;
esac
