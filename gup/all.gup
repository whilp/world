#!/bin/sh

set -eux

chmod 700 .ssh
chmod 700 .gnupg

gup -u \
    .ssh/config \
    .gnupg/gpg.conf \
    .gnupg/gpg-agent.conf \
    .python/packages \
    bin/git-get \
    bin/hub \
    bin/glide \
    bin/rg \
    bin/vault \
    prefix/vis
