#!/bin/sh

set -eux

chmod 700 .ssh
chmod 700 .gnupg

gup -u \
    Makefile \
    tools/gup \
    .emacs.d \
    .elpa \
    .ssh/config \
    .gnupg/gpg.conf \
    .gnupg/gpg-agent.conf \
    .python/packages \
    bin/git-get \
    bin/hub \
    bin/glide \
    bin/vault
