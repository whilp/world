Clue{
  name = "All-in-one capture",
  desc = "CleanShot all-in-one capture",
  key = { "hyper", "c", "a" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://all-in-one" }
}

Clue{
  name = "Capture area",
  desc = "CleanShot capture area",
  key = { "hyper", "c", "c" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://capture-area" }
}

Clue{
  name = "Capture fullscreen",
  desc = "CleanShot capture fullscreen",
  key = { "hyper", "c", "f" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://capture-fullscreen" }
}

Clue{
  name = "Capture window",
  desc = "CleanShot capture window",
  key = { "hyper", "c", "w" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://capture-window" }
}

Clue{
  name = "Capture scrolling window",
  desc = "CleanShot capture scrolling window",
  key = { "hyper", "c", "s" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://scrolling-capture" }
}

Clue{
  name = "Record screen",
  desc = "CleanShot record screen",
  key = { "hyper", "c", "r" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://record-screen" }
}

Clue{
  name = "OCR text from screen",
  desc = "CleanShot OCR text from screen",
  key = { "hyper", "c", "o" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://capture-text" }
}

Clue{
  name = "Open capture history",
  desc = "CleanShot open capture history",
  key = { "hyper", "c", "h" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://open-history" }
}

Clue{
  name = "Toggle desktop icons",
  desc = "CleanShot toggle desktop icons",
  key = { "hyper", "c", "d" },
  group = "cleanshot",
  show_in_chooser = true,
  action = { url = "cleanshot://toggle-desktop-icons" }
}
